/*
 * Stripy Horse API
 * Zebra/ZPL developer tools: render ZPL to PNG, convert PDFs and images to print-ready ZPL, and drive hosted virtual Zebra printers for testing. Authenticate with an API key via `Authorization: Bearer sh_live_…` or `X-Api-Key`.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.stripyhorse.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.stripyhorse.model.Finding;
import io.stripyhorse.model.Report;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.stripyhorse.JSON;

/**
 * PreflightOutputBody
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-08-29T23:14:08.633770422Z[Etc/UTC]", comments = "Generator version: 7.24.0")
public class PreflightOutputBody {
  public static final String SERIALIZED_NAME_LABELS = "labels";
  @SerializedName(SERIALIZED_NAME_LABELS)
  @javax.annotation.Nullable
  private List<Report> labels;

  public static final String SERIALIZED_NAME_LINT = "lint";
  @SerializedName(SERIALIZED_NAME_LINT)
  @javax.annotation.Nullable
  private List<Finding> lint;

  public PreflightOutputBody() {
  }

  public PreflightOutputBody labels(@javax.annotation.Nullable List<Report> labels) {
    this.labels = labels;
    return this;
  }

  public PreflightOutputBody addLabelsItem(Report labelsItem) {
    if (this.labels == null) {
      this.labels = new ArrayList<>();
    }
    this.labels.add(labelsItem);
    return this;
  }

  /**
   * One report per rendered label
   * @return labels
   */
  @javax.annotation.Nullable
  public List<Report> getLabels() {
    return labels;
  }

  public void setLabels(@javax.annotation.Nullable List<Report> labels) {
    this.labels = labels;
  }


  public PreflightOutputBody lint(@javax.annotation.Nullable List<Finding> lint) {
    this.lint = lint;
    return this;
  }

  public PreflightOutputBody addLintItem(Finding lintItem) {
    if (this.lint == null) {
      this.lint = new ArrayList<>();
    }
    this.lint.add(lintItem);
    return this;
  }

  /**
   * Static ZPL findings: structure, encoding, bounds
   * @return lint
   */
  @javax.annotation.Nullable
  public List<Finding> getLint() {
    return lint;
  }

  public void setLint(@javax.annotation.Nullable List<Finding> lint) {
    this.lint = lint;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    PreflightOutputBody preflightOutputBody = (PreflightOutputBody) o;
    return Objects.equals(this.labels, preflightOutputBody.labels) &&
        Objects.equals(this.lint, preflightOutputBody.lint);
  }

  @Override
  public int hashCode() {
    return Objects.hash(labels, lint);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class PreflightOutputBody {\n");
    sb.append("    labels: ").append(toIndentedString(labels)).append("\n");
    sb.append("    lint: ").append(toIndentedString(lint)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("labels", "lint"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("labels", "lint"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to PreflightOutputBody
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!PreflightOutputBody.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in PreflightOutputBody is not found in the empty JSON string", PreflightOutputBody.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!PreflightOutputBody.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `PreflightOutputBody` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : PreflightOutputBody.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("labels") != null && !jsonObj.get("labels").isJsonNull()) {
        if (!jsonObj.get("labels").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `labels` to be an array in the JSON string but got `%s`", jsonObj.get("labels").toString()));
        }
        JsonArray jsonArraylabels = jsonObj.getAsJsonArray("labels");
        // validate the required field `labels` (array)
        for (int i = 0; i < jsonArraylabels.size(); i++) {
          Report.validateJsonElement(jsonArraylabels.get(i));
        }
      }
      if (jsonObj.get("lint") != null && !jsonObj.get("lint").isJsonNull()) {
        if (!jsonObj.get("lint").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `lint` to be an array in the JSON string but got `%s`", jsonObj.get("lint").toString()));
        }
        JsonArray jsonArraylint = jsonObj.getAsJsonArray("lint");
        // validate the required field `lint` (array)
        for (int i = 0; i < jsonArraylint.size(); i++) {
          Finding.validateJsonElement(jsonArraylint.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!PreflightOutputBody.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'PreflightOutputBody' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<PreflightOutputBody> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(PreflightOutputBody.class));

       return (TypeAdapter<T>) new TypeAdapter<PreflightOutputBody>() {
           @Override
           public void write(JsonWriter out, PreflightOutputBody value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public PreflightOutputBody read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of PreflightOutputBody given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of PreflightOutputBody
   * @throws IOException if the JSON string is invalid with respect to PreflightOutputBody
   */
  public static PreflightOutputBody fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, PreflightOutputBody.class);
  }

  /**
   * Convert an instance of PreflightOutputBody to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

